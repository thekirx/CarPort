import Image from "next/image";
import { builds, reviews } from "@/data/proof";
export default function BuildsPage() { return <><section className="page-hero"><div className="container"><span className="eyebrow light">AUTHENTIC CARPORT PHOTOGRAPHY</span><h1>Shop floor,<br />not stock photos.</h1><p>Real imagery from Carport’s social gallery. Captions and reviews remain illustrative until approved by the client.</p></div></section><section className="section concrete"><div className="container build-grid large">{builds.map((build) => <figure key={build.image}><Image src={build.image} alt={`${build.vehicle} at Carport Wheels`} fill sizes="(max-width: 700px) 90vw, 33vw" /><figcaption><span className="sample-badge">ILLUSTRATIVE CAPTION</span><b>{build.vehicle}</b><small>{build.detail}</small></figcaption></figure>)}</div></section><section className="section asphalt"><div className="container review-grid">{reviews.map((review) => <blockquote key={review.name}><span className="sample-badge">SAMPLE REVIEW</span><p>“{review.quote}”</p><footer><b>{review.name}</b><span>{review.vehicle}</span></footer></blockquote>)}</div></section></>; }
